# خطة تقنية: نظام إدارة صفحات التواصل الاجتماعي بالذكاء الاصطناعي

> **الهدف:** بناء نظام بلغة **بايثون** يدير صفحة **فيسبوك** وحساب **إنستغرام** (Business) لبيع منتجات معيّنة،
> يولّد **الصور والفيديوهات والنصوص** تلقائيًا بالذكاء الاصطناعي، وينشرها **بجدولة زمنية** محددة.
>
> **حالة الوثيقة:** خطة معمارية أولية (قبل كتابة الكود). جميع مفاتيح الخدمات المدفوعة تُترك **أماكن فارغة** في `.env`.

---

## 1) هل هذا ممكن وقانوني؟

نعم — بشرط استخدام **الواجهات البرمجية الرسمية (APIs)** من Meta وليس أتمتة تسجيل الدخول كإنسان.

- ✅ **مسموح:** النشر والجدولة عبر **Graph API** لصفحات فيسبوك وحسابات إنستغرام التجارية.
- ✅ **مسموح:** توليد المحتوى بالذكاء الاصطناعي عبر خدمات مثل OpenAI / Runway / Stability.
- ⚠️ **يتطلب:** حساب إنستغرام **Business/Creator** مربوطًا بصفحة فيسبوك.
- ⚠️ **يتطلب:** تطبيق Meta Developer + **مراجعة التطبيق (App Review)** لبعض الصلاحيات قبل التشغيل الحقيقي.
- ❌ **ممنوع:** استخدام أدوات تتحكم بالحساب بكلمة المرور مباشرة (تُعرّض الحساب للحظر).

---

## 2) اللغة والتقنيات المختارة

| الطبقة | التقنية | السبب |
|--------|---------|-------|
| اللغة الأساسية | **Python 3.11+** | أنضج نظام لمكتبات الذكاء الاصطناعي والجدولة |
| إطار الخدمة/الـ API | **FastAPI** | خفيف، سريع، توثيق تلقائي (Swagger) |
| الجدولة | **APScheduler** (بسيط) أو **Celery + Redis** (احترافي) | تشغيل النشر في مواعيد محددة |
| قاعدة البيانات | **PostgreSQL** (أو SQLite للتجربة) | تخزين المنتجات والمنشورات والجدول والنتائج |
| ORM | **SQLAlchemy + Alembic** | نمذجة الجداول والترحيلات |
| لوحة تحكم (اختياري) | HTML + HTMX أو Next.js | مراجعة/اعتماد المحتوى قبل النشر |
| النشر (Deployment) | Docker + خادم VPS أو خدمة سحابية | تشغيل دائم للمجدوِل |

> **ملاحظة عن HTML:** ليست لغة برمجة كاملة — تُستخدم فقط لواجهة لوحة التحكم في المتصفح، وتُشغَّل خلفها بايثون.

---

## 3) المعمارية العامة (تدفّق العمل)

```
┌──────────────┐   ┌─────────────────────┐   ┌──────────────────┐
│  منتج جديد   │──▶│  مولّد المحتوى (AI)  │──▶│  قائمة المسودّات  │
│ (اسم/سعر/    │   │  نص + صورة + فيديو  │   │  (بانتظار موعد)  │
│  وصف/صورة)   │   └─────────────────────┘   └────────┬─────────┘
└──────────────┘                                       │
                                                       ▼
┌──────────────┐   ┌─────────────────────┐   ┌──────────────────┐
│  تحليل النتائج│◀──│   ناشر Graph API    │◀──│    المجدوِل      │
│ (وصول/تفاعل) │   │ فيسبوك + إنستغرام   │   │ (Scheduler cron) │
└──────────────┘   └─────────────────────┘   └──────────────────┘
```

**الخطوات:**
1. تُدخل بيانات المنتج (يدويًا أو من ملف/قاعدة بيانات).
2. طبقة الذكاء الاصطناعي تولّد: نص المنشور + الهاشتاغ + صورة (وفيديو ريلز اختياري).
3. تُحفظ المسودّة مع **موعد النشر** في قاعدة البيانات.
4. (اختياري) مراجعة بشرية للاعتماد عبر لوحة التحكم.
5. المجدوِل يلتقط المنشورات المستحقّة وينشرها عبر Graph API.
6. تُسجَّل نتائج الأداء (وصول/تفاعل) لتحسين المحتوى لاحقًا.

---

## 4) مكوّنات الذكاء الاصطناعي

### 4.1 توليد النصوص (كابشن + هاشتاغ)
- **الخدمة:** Claude API أو OpenAI GPT.
- **المدخل:** اسم المنتج + وصفه + السعر + نبرة العلامة التجارية.
- **المخرج:** كابشن عربي جذّاب + 5–15 هاشتاغ + Call-to-Action.
- **التكلفة:** منخفضة جدًا (أجزاء من السنت لكل منشور).

### 4.2 توليد الصور
- **الخدمات:** OpenAI `gpt-image` / DALL·E، Google Imagen، Stability AI، Flux.
- **المدخل:** برومبت يوصف المنتج + خلفية + أسلوب بصري متسق مع الهوية.
- **الاعتبارات:** صورة المنتج الحقيقية أفضل تجاريًا؛ الـ AI مفيد للخلفيات والإعلانات التصميمية.
- **التكلفة:** ~ 2–8 سنت لكل صورة حسب الجودة.

### 4.3 توليد الفيديو (الأصعب والأغلى)
- **الخدمات:** Runway، Google Veo، Kling، Luma، Sora API.
- **الواقع:** مقاطع قصيرة (ثوانٍ)، مناسبة للريلز/القصص، تكلفة أعلى ووقت معالجة أطول.
- **بديل عملي أرخص:** تركيب فيديو من صور المنتج + موسيقى + نص متحرك عبر **MoviePy / ffmpeg** بدل توليد كامل بالـ AI.

---

## 5) الربط مع Meta (فيسبوك + إنستغرام)

### 5.1 المتطلبات المسبقة
1. حساب **Meta for Developers** وإنشاء **App** من نوع Business.
2. صفحة فيسبوك للعلامة التجارية.
3. حساب إنستغرام **Business** مربوط بالصفحة (عبر إعدادات إنستغرام → حساب أعمال).
4. توليد **Page Access Token** طويل الأمد + الصلاحيات:
   `pages_manage_posts`, `pages_read_engagement`, `instagram_basic`, `instagram_content_publish`.

### 5.2 النشر على فيسبوك
- نقطة النهاية: `POST /{page-id}/photos` أو `/{page-id}/videos` أو `/{page-id}/feed`.
- الجدولة المدمجة مدعومة عبر `published=false` + `scheduled_publish_time`.

### 5.3 النشر على إنستغرام (خطوتان)
1. إنشاء **حاوية وسائط**: `POST /{ig-user-id}/media` (برابط الصورة/الفيديو + الكابشن).
2. **النشر**: `POST /{ig-user-id}/media_publish` (بمعرّف الحاوية).
- ملاحظة: إنستغرام يتطلب أن تكون الوسائط على **رابط عام** (يُرفع أولًا لتخزين سحابي مثل S3).

---

## 6) الجدولة التلقائية

- **APScheduler** يشغّل مهمة كل دقيقة تتفحّص قاعدة البيانات عن منشورات موعدها حان → ينشرها.
- دعم أنماط: وقت محدد، تكرار يومي/أسبوعي، أفضل أوقات التفاعل.
- حماية من التكرار (idempotency) ومن **حدود المعدل (Rate Limits)** لدى Meta.

---

## 7) مخطط قاعدة البيانات (مبدئي)

```
products         (id, name, description, price, image_url, created_at)
generated_assets (id, product_id, type[text/image/video], content_url, prompt, created_at)
posts            (id, product_id, caption, media_url, platform[fb/ig/both],
                  status[draft/approved/scheduled/published/failed],
                  scheduled_at, published_at, external_post_id)
analytics        (id, post_id, reach, likes, comments, shares, fetched_at)
settings         (key, value)   -- مثل نبرة العلامة، الهاشتاغ الثابت
```

---

## 8) هيكل المشروع المقترح

```
social_automation/
├── .env.example            # قالب المفاتيح (أماكن فارغة)
├── requirements.txt
├── docker-compose.yml
├── app/
│   ├── main.py             # FastAPI: نقاط النهاية + تشغيل المجدوِل
│   ├── config.py           # قراءة المتغيّرات من .env
│   ├── database.py         # اتصال SQLAlchemy
│   ├── models.py           # جداول قاعدة البيانات
│   ├── scheduler.py        # APScheduler + مهمة النشر الدورية
│   ├── ai/
│   │   ├── text.py         # توليد الكابشن والهاشتاغ
│   │   ├── image.py        # توليد الصور
│   │   └── video.py        # توليد/تركيب الفيديو
│   ├── publishers/
│   │   ├── facebook.py     # Graph API لفيسبوك
│   │   └── instagram.py    # Graph API لإنستغرام
│   ├── storage.py          # رفع الوسائط لتخزين عام (S3/غيره)
│   └── services.py         # منطق التنسيق بين المكوّنات
└── tests/
```

---

## 9) قالب المفاتيح `.env.example` (أماكن فارغة)

```env
# ---- Meta / Facebook + Instagram ----
META_APP_ID=
META_APP_SECRET=
FB_PAGE_ID=
FB_PAGE_ACCESS_TOKEN=
IG_BUSINESS_ACCOUNT_ID=

# ---- توليد النصوص ----
ANTHROPIC_API_KEY=
# أو
OPENAI_API_KEY=

# ---- توليد الصور ----
IMAGE_API_KEY=

# ---- توليد الفيديو ----
VIDEO_API_KEY=

# ---- تخزين الوسائط العام (لإنستغرام) ----
S3_BUCKET=
S3_ACCESS_KEY=
S3_SECRET_KEY=
S3_REGION=

# ---- قاعدة البيانات ----
DATABASE_URL=postgresql://user:pass@localhost:5432/social
```

---

## 10) التكاليف التقديرية (شهريًا)

| البند | تقدير |
|-------|-------|
| توليد النصوص | 1–5 دولار (حسب الكمية) |
| توليد الصور | 10–50 دولار |
| توليد الفيديو (AI) | 30–200+ دولار (الأغلى) |
| خادم VPS للتشغيل الدائم | 5–20 دولار |
| تخزين S3 | 1–5 دولار |
| **واجهات Meta** | **مجانية** ضمن الحدود |

> نصيحة: ابدأ بالنصوص + الصور، وأجّل فيديو الـ AI حتى تتأكد من العائد.

---

## 11) القيود والامتثال (اقرأها جيدًا)

1. إنستغرام يتطلب حساب **Business** — لا يعمل مع الحساب الشخصي.
2. **App Review** من Meta ضروري قبل التشغيل الحقيقي بصلاحيات النشر.
3. فيديوهات الـ AI قصيرة ومكلفة نسبيًا — الأنسب للريلز.
4. **حدود المعدل** — لا نشر بكميات ضخمة فوريًا.
5. **مراجعة بشرية مستحبّة** قبل النشر الآلي لحماية سمعة العلامة.
6. الالتزام بسياسات المحتوى والإعلان لدى Meta (خاصة للمنتجات الحساسة كالأدوية والمكمّلات).

---

## 12) خارطة الطريق (مراحل التنفيذ)

| المرحلة | المخرَج |
|---------|---------|
| **1. الأساس** | هيكل المشروع + قاعدة البيانات + `.env.example` + FastAPI يعمل |
| **2. الذكاء الاصطناعي** | توليد النص + الصورة لمنتج (بدون نشر) |
| **3. النشر** | ربط فيسبوك وإنستغرام + نشر مسودّة واحدة يدويًا |
| **4. الجدولة** | APScheduler ينشر تلقائيًا في المواعيد |
| **5. لوحة التحكم** | مراجعة/اعتماد المحتوى قبل النشر |
| **6. الفيديو والتحليلات** | تركيب/توليد الفيديو + جمع نتائج الأداء |

---

## الخطوة التالية

عند الموافقة على هذه الخطة، يمكن البدء بـ **المرحلة 1**: إنشاء هيكل مشروع بايثون فعلي داخل مجلد
`social_automation/` مع قاعدة البيانات و`.env.example` وخدمة FastAPI جاهزة للتشغيل.
